# Secrets
.env

# Python env
.venv/
__pycache__/
*.pyc

# AppWorld runtime artifacts (downloaded / generated locally)
data/
experiments/
logs/

# Derived API catalog + HydraDB ingest marker (rebuilt locally on first run)
.api_catalog.json
.api_hydra_ingested
